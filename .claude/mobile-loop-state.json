{
  "iteration": 104,
  "cycle": 3,
  "perspectiveIndex": 22,
  "target": 154,
  "batch": "mobile-live-qa-followup",
  "consecutiveCleanIterations": 0,
  "liveDevice": "QA_Pixel6 emulator, booted, expo run:android connected via Metro (adb + uiautomator for verification)",
  "history": [
    {
      "iteration": 101,
      "perspective": "Android hardware back-button, live on a real device for the first time (B9 revisited)",
      "outcome": "CLEAN, live-verified for the first time via a real Android emulator (QA_Pixel6): correct pop/switch-to-home/exit pattern at three navigation depths, confirmed via adb keyevent + dumpsys. Previously only verified by reading source."
    },
    {
      "iteration": 102,
      "perspective": "Kill-and-restore state integrity, a real process kill instead of a code-only argument (B13 revisited)",
      "outcome": "CLEAN, live-verified via a genuine adb am kill (confirmed dead via pidof, not just backgrounded). Reading-progress data survived intact; exact in-stack screen not restored, which is standard Android behavior, not a regression."
    },
    {
      "iteration": 103,
      "perspective": "Deep link handling including malformed links, live on a real device (B10 revisited)",
      "outcome": "GAP FOUND AND FIXED: a malformed surah/* deep link left the header showing the previous, successfully-loaded surah's title instead of clearing it, because SurahReaderScreen.tsx's header-setting useLayoutEffect never ran on the error path. Fixed by adding an explicit error branch; live re-verified the exact repro is clean after the fix. Found only because a live screenshot was compared, not just a crash/no-crash check."
    },
    {
      "iteration": 104,
      "perspective": "Sync engine mobile edge cases, live this time (B23 revisited)",
      "outcome": "GAP FOUND AND FIXED, a real one: generating a recovery phrase threw an uncaught error unconditionally in every dev-client build with New Architecture enabled, regardless of remote-debugging state, because the CSPRNG guard's detection heuristic (nativeCallSyncHook absence) is always true under New Architecture. Sync was untestable in any dev/QA build before this fix. Switched to expo-crypto's getRandomBytesAsync, which has no such fallback ever, removing both the false positive and the underlying weak-randomness risk it was guarding against. Live re-verified: phrase generation, turning sync on, and a genuine mid-sync-round process kill all clean afterward."
    }
  ]
}
